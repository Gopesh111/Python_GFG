class Solution:
    def findMaxConsecutiveOnes(self, nums: List[int]) -> int:
        count=0
        maxcount=0
        for ele in nums:
            if ele==1:
                count+=1
                maxcount=max(maxcount,count)
            else:
                count=0
        return maxcount
