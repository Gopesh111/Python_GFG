a = input()
freq = {}
found = False

for ch in a:
    freq[ch] = freq.get(ch, 0) + 1

for ch in a:
    if freq[ch] == 1:
        print(ch)
        found = True
        break

if not found:
    print("No non-repeating character")
