class Solution:
    def nonRepeatingChar(self,s):
        #code here
        d={}
        for char in s:
            d[char]=d.get(char,0)+1
        for char in s:
            if d[char]==1:
                return char
        return -1
           
    
    
